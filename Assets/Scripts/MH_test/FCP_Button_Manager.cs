using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class FCP_Button_Manager : MonoBehaviour
{
    //Array of FCP Buttons
    public Button StatButton;
    public Button DynButton;
    public Button RCP45Button;
    public Button RCP85Button;
    public Button AbsChangeButton;
    public Button ValueButton;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
